
b1 = 1;
b2 = 1;
order = 4;
BC = 2;
N = 21;
ratios = linspace(0,1,100);
eigs = zeros(3,length(ratios));

for i = 1:3
  for j = 1:length(ratios)  
    eigs(i,j) = bigboy(i,N,ratios(j),BC,order,b1,b2); 
  end
end

plot(